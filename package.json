{
  "name": "eight-west-trail",
  "private": true,
  "version": "0.1.0",
  "description": "The 8 West Trail — a road-trip survival game from 8 West IT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
