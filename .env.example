# Copy to .env on coastline (~/apps/eight-west-trail/.env). Never commit .env.
# Cloudflare Turnstile secret for the 8wt.8westit.com widget.
TURNSTILE_SECRET=
# openssl rand -hex 32 — salts the daily IP hash used for rate limits and reports.
IP_HASH_SECRET=
